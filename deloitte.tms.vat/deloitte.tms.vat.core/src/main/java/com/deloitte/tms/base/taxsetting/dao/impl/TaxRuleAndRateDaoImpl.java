package com.deloitte.tms.base.taxsetting.dao.impl;
// Generated by bo.wang with ling2.autoproject

import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Component;

import com.deloitte.tms.pl.core.commons.support.DaoPage;
import com.deloitte.tms.pl.core.commons.utils.AssertHelper;
import com.deloitte.tms.pl.core.dao.impl.BaseDao;
import com.deloitte.tms.base.taxsetting.model.TaxRuleAndRate;
import com.deloitte.tms.base.taxsetting.dao.TaxRuleAndRateDao;
/**
 * Home object for domain model class TaxRuleAndRate.
 * @see com.deloitte.tms.base.taxsetting.model
 * @author Hibernate Tools
 */
@Component(TaxRuleAndRateDao.BEAN_ID)
public class TaxRuleAndRateDaoImpl extends BaseDao<TaxRuleAndRate> implements TaxRuleAndRateDao{
	public DaoPage findTaxRuleAndRateByParams(Map params, Integer pageIndex,Integer pageSize)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildTaxRuleAndRateQuery(query, values, params);
		return pageBy(query, values, pageIndex, pageSize);
	}
	public List<TaxRuleAndRate> findTaxRuleAndRateByParams(Map params)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildTaxRuleAndRateQuery(query, values, params);
		return findBy(query, values);
	}
	private void buildTaxRuleAndRateQuery(StringBuffer query,Map values,Map params) {
		query.append(" from TaxRuleAndRate where 1=1 ");
		Object value=params.get("status");
		if(value!=null)
		{
			query.append(" and status=:status");
			values.put("status", value);
		}
	}
}

