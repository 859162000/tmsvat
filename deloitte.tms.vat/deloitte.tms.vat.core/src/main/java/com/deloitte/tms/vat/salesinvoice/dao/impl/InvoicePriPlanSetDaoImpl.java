package com.deloitte.tms.vat.salesinvoice.dao.impl;
// Generated by bo.wang with ling2.autoproject

import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Component;

import com.deloitte.tms.pl.core.commons.support.DaoPage;
import com.deloitte.tms.pl.core.commons.utils.AssertHelper;
import com.deloitte.tms.pl.core.dao.impl.BaseDao;
import com.deloitte.tms.vat.salesinvoice.model.InvoicePriPlanSet;
import com.deloitte.tms.vat.salesinvoice.dao.InvoicePriPlanSetDao;
/**
 * Home object for domain model class InvoicePriPlanSet.
 * @see com.deloitte.tms.vat.salesinvoice.model
 * @author Hibernate Tools
 */
@Component(InvoicePriPlanSetDao.BEAN_ID)
public class InvoicePriPlanSetDaoImpl extends BaseDao<InvoicePriPlanSet> implements InvoicePriPlanSetDao{
	public DaoPage findInvoicePriPlanSetByParams(Map params, Integer pageIndex,Integer pageSize)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildInvoicePriPlanSetQuery(query, values, params);
		return pageBy(query, values, pageIndex, pageSize);
	}
	public List<InvoicePriPlanSet> findInvoicePriPlanSetByParams(Map params)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildInvoicePriPlanSetQuery(query, values, params);
		return findBy(query, values);
	}
	private void buildInvoicePriPlanSetQuery(StringBuffer query,Map values,Map params) {
		query.append(" from InvoicePriPlanSet where 1=1 ");
		Object value=params.get("status");
		if(value!=null)
		{
			query.append(" and status=:status");
			values.put("status", value);
		}
	}
	
	public int getNumByCode(String[] codes){
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		query.append("select count(1) from InvoicePriPlanSet where 1=1 ");
		query.append(" and codes in :codes");
		values.put("codes", codes);
		return findNumhql(query, values);
				
	}
}

