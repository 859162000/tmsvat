package com.deloitte.tms.vat.salesinvoice.dao.impl;
// Generated by bo.wang with ling2.autoproject

import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Component;

import com.deloitte.tms.pl.core.commons.support.DaoPage;
import com.deloitte.tms.pl.core.commons.utils.AssertHelper;
import com.deloitte.tms.pl.core.dao.impl.BaseDao;
import com.deloitte.tms.vat.salesinvoice.model.InvoicePrintPoolD;
import com.deloitte.tms.vat.salesinvoice.dao.InvoicePrintPoolDDao;
/**
 * Home object for domain model class InvoicePrintPoolD.
 * @see com.deloitte.tms.vat.salesinvoice.model
 * @author Hibernate Tools
 */
@Component(InvoicePrintPoolDDao.BEAN_ID)
public class InvoicePrintPoolDDaoImpl extends BaseDao<InvoicePrintPoolD> implements InvoicePrintPoolDDao{
	public DaoPage findInvoicePrintPoolDByParams(Map params, Integer pageIndex,Integer pageSize)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildInvoicePrintPoolDQuery(query, values, params);
		return pageBy(query, values, pageIndex, pageSize);
	}
	public List<InvoicePrintPoolD> findInvoicePrintPoolDByParams(Map params)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildInvoicePrintPoolDQuery(query, values, params);
		return findBy(query, values);
	}
	private void buildInvoicePrintPoolDQuery(StringBuffer query,Map values,Map params) {
		query.append(" from InvoicePrintPoolD where 1=1 ");
		Object value=params.get("status");
		if(value!=null)
		{
			query.append(" and status=:status");
			values.put("status", value);
		}
		value=params.get("invoicePrtPoolLId");
		if(value!=null)
		{
			query.append(" and invoicePrtPoolLId=:invoicePrtPoolLId");
			values.put("invoicePrtPoolLId", value);
		}
	}
}

