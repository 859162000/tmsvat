package com.deloitte.tms.base.masterdata.dao.impl;
// Generated by bo.wang with ling2.autoproject

import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Component;

import com.deloitte.tms.pl.core.commons.support.DaoPage;
import com.deloitte.tms.pl.core.commons.utils.AssertHelper;
import com.deloitte.tms.pl.core.dao.impl.BaseDao;
import com.deloitte.tms.base.masterdata.model.TmsMdLegalEntity;
import com.deloitte.tms.base.masterdata.dao.TmsMdLegalEntityDao;
/**
 * Home object for domain model class TmsMdLegalEntity.
 * @see com.deloitte.tms.base.masterdata.model
 * @author Hibernate Tools
 */
@Component(TmsMdLegalEntityDao.BEAN_ID)
public class TmsMdLegalEntityDaoImpl extends BaseDao<TmsMdLegalEntity> implements TmsMdLegalEntityDao{
	public DaoPage findTmsMdLegalEntityByParams(Map params, Integer pageIndex,Integer pageSize)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildTmsMdLegalEntityQuery(query, values, params);
		return pageBy(query, values, pageIndex, pageSize);
	}
	public List<TmsMdLegalEntity> findTmsMdLegalEntityByParams(Map params)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildTmsMdLegalEntityQuery(query, values, params);
		return findBy(query, values);
	}
	private void buildTmsMdLegalEntityQuery(StringBuffer query,Map values,Map params) {
		query.append(" from TmsMdLegalEntity where 1=1 ");
		Object value=params.get("status");
		if(AssertHelper.isOrNotEmpty_assert(value))
		{
			query.append(" and status=:status");
			values.put("status", value);
		}
		if(AssertHelper.isOrNotEmpty_assert(params.get("legalEntityCode")))
		{
			query.append(" and legalEntityCode like :legalEntityCode");
			values.put("legalEntityCode", "%"+params.get("legalEntityCode")+"%");
		}
	}
	public List<TmsMdLegalEntity> findAllTmsMdLegalEntity(){
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		query.append(" from TmsMdLegalEntity where 1=1 ");
		return findBy(query, values);
	}
}

