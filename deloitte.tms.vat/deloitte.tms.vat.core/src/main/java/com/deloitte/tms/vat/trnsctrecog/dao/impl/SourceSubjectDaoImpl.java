package com.deloitte.tms.vat.trnsctrecog.dao.impl;
// Generated by bo.wang with ling2.autoproject

import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Component;

import com.deloitte.tms.pl.core.commons.constant.DictionaryDef;
import com.deloitte.tms.pl.core.commons.support.DaoPage;
import com.deloitte.tms.pl.core.commons.utils.AssertHelper;
import com.deloitte.tms.pl.core.dao.impl.BaseDao;
import com.deloitte.tms.vat.trnsctrecog.dao.ISourceSubjectDao;
import com.deloitte.tms.vat.trnsctrecog.model.SourceSubject;
/**
 * Home object for domain model class SourceSubject.
 * @see com.deloitte.tms.vat.bill.pm.model
 * @author Hibernate Tools
 */
@Component(ISourceSubjectDao.BEAN_ID)
public class SourceSubjectDaoImpl extends BaseDao<SourceSubject> implements ISourceSubjectDao{
	public DaoPage findSourceSubjectByParams(Map params, Integer pageIndex,Integer pageSize)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildSourceSubjectQuery(query, values, params);
		return pageBy(query, values, pageIndex, pageSize);
	}
	public DaoPage findSourceSubjectPartByParams(Map params, Integer pageIndex,Integer pageSize)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildSourceSubjectPartQuery(query, values, params);
		return pageBy(query, values, pageIndex, pageSize);
	}
	public List<SourceSubject> findSourceSubjectByParams(Map params)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildSourceSubjectQuery(query, values, params);
		return findBy(query, values);
	}
	private void buildSourceSubjectQuery(StringBuffer query,Map values,Map params) {
		query.append(" from SourceSubject where 1=1 ");
		Object value=params.get("code");
		if(AssertHelper.notEmpty(value))
		{
			query.append(" and status=:status");
			values.put("status", value);
		}
	}
	private void buildSourceSubjectPartQuery(StringBuffer query,Map values,Map params) {
		query.append(" from SourceSubjectPart where 1=1 ");
		Object value=params.get("code");
		if(AssertHelper.notEmpty(value))
		{
			query.append(" and status=:status");
			values.put("status", value);
		}
	}
}

