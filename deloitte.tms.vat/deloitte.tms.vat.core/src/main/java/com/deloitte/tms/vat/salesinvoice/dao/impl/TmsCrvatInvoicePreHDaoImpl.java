package com.deloitte.tms.vat.salesinvoice.dao.impl;
// Generated by bo.wang with ling2.autoproject

import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.springframework.stereotype.Component;

import com.deloitte.tms.pl.core.commons.support.DaoPage;
import com.deloitte.tms.pl.core.commons.utils.AssertHelper;
import com.deloitte.tms.pl.core.dao.impl.BaseDao;
import com.deloitte.tms.vat.salesinvoice.model.TmsCrvatInvoicePreH;
import com.deloitte.tms.vat.salesinvoice.dao.TmsCrvatInvoicePreHDao;
import com.deloitte.tms.vat.salesinvoice.model.TmsCrvatInvoicePreL;
/**
 * Home object for domain model class TmsCrvatInvoicePreH.
 * @see com.deloitte.tms.vat.salesinvoice.model
 * @author Hibernate Tools
 */
@Component(TmsCrvatInvoicePreHDao.BEAN_ID)
public class TmsCrvatInvoicePreHDaoImpl extends BaseDao<TmsCrvatInvoicePreH> implements TmsCrvatInvoicePreHDao{
	public DaoPage findTmsCrvatInvoicePreHByParams(Map params, Integer pageIndex,Integer pageSize)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildTmsCrvatInvoicePreHQuery(query, values, params);
		return pageBy(query, values, pageIndex, pageSize);
	}
	public List<TmsCrvatInvoicePreH> findTmsCrvatInvoicePreHByParams(Map params)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildTmsCrvatInvoicePreHQuery(query, values, params);
		return findBy(query, values);
	}
	private void buildTmsCrvatInvoicePreHQuery(StringBuffer query,Map values,Map params) {
		query.append(" from TmsCrvatInvoicePreH where 1=1 ");
		Object value=params.get("status");
		if(value!=null)
		{
			query.append(" and status=:status");
			values.put("status", value);
		}
		value=params.get("approvalStatus");
		if(value!=null)
		{
			query.append(" and approvalStatus=:approvalStatus");
			values.put("approvalStatus", value);
		}
	}
	public DaoPage findTmsCrvatInvoicePreLByParams(Map params, Integer pageIndex,Integer pageSize)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildTmsCrvatInvoicePreLQuery(query, values, params);
		return pageBy(query, values, pageIndex, pageSize);
	}
	public List<TmsCrvatInvoicePreL> findTmsCrvatInvoicePreLByParams(Map params)
	{
		StringBuffer query=new StringBuffer();
		Map values=new HashMap();
		buildTmsCrvatInvoicePreLQuery(query, values, params);
		return findBy(query, values);
	}
	private void buildTmsCrvatInvoicePreLQuery(StringBuffer query,Map values,Map params) {
		query.append(" from TmsCrvatInvoicePreL where 1=1 ");
		Object value=params.get("tmsCrvatInvoicePreHId");
		if(value!=null&&!"".equals(value))
		{
			value=value.toString();
			query.append(" and invoicePeHid=:tmsCrvatInvoicePreHId");
			values.put("tmsCrvatInvoicePreHId", value);
		}
		value=params.get("status");
		if(value!=null)
		{
			query.append(" and status=:status");
			values.put("status", value);
		}
	}
}

